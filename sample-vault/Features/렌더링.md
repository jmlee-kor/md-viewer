# 렌더링 기능 데모

이 노트에는 #데모 #렌더링 태그가 달려 있습니다. 태그를 클릭하면 같은 태그의 노트가 사이드바에 모입니다.

> [!note] 안내
> 우측 상단 **≣** 버튼으로 이 노트의 **목차(TOC)** 를 열 수 있습니다. 헤딩이 많을수록 유용합니다.

## 콜아웃 (Admonition)

> [!tip] 팁
> `> [!tip] 제목` 형식으로 콜아웃을 만듭니다. 타입별로 색이 다릅니다.

> [!warning] 주의
> 경고는 노란색 계열입니다.

> [!danger] 위험
> 위험/에러는 빨간색 계열입니다.

## 수식 (KaTeX)

인라인 수식: 질량-에너지 등가는 $E = mc^2$ 입니다.

블록 수식:

$$
\int_{a}^{b} f(x)\,dx = F(b) - F(a)
$$

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

## 코드 하이라이트

JavaScript:

```js
function fib(n) {
  return n < 2 ? n : fib(n - 1) + fib(n - 2);
}
console.log(fib(10)); // 55
```

Python:

```python
def greet(name: str) -> str:
    return f"안녕, {name}!"

print(greet("세계"))
```

## 임베드 (Transclusion)

이미지 임베드: `![[assets/logo.svg]]`

![[assets/logo.svg]]

노트 임베드 (`![[Projects/Roadmap]]`):

![[Projects/Roadmap]]

## 더 둘러보기

- [[Diagrams]] — 다이어그램 4종
- [[Slides]] — Marp 슬라이드 (전체화면 `F` · 발표자 `P`)
- [[Welcome]] 로 돌아가기

> 💡 **Ctrl+P** 로 어디서든 파일을 빠르게 열고, 사이드바 검색으로 본문 전문 검색을 해보세요.
